import { Request, Response, Router } from 'express';

const router = Router();

// router.post('/login', (req: Request, res: Response) => login(req, res));
// router.post('/register', (req: Request, res: Response) => register(req, res));
// router.get('/allUsers/:id', (req: Request, res: Response) => getAllUsers(req, res));
// router.post('/setAvatar/:id', (req: Request, res: Response) => setAvatar(req, res));
// router.get('/logout/:id', (req: Request, res: Response) => logOut(req, res));

export default router;
